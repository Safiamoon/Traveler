{% extends 'base.html.twig' %}

{% block title %}Voyage{% endblock %}

{% block body %}
    <h1>Voyage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voyage.id }}</td>
            </tr>
            <tr>
                <th>DateVoyage</th>
                <td>{{ voyage.DateVoyage ? voyage.DateVoyage|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ voyage.note }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ voyage.description }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ voyage.titre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_voyage_index') }}">back to list</a>

    <a href="{{ path('admin_voyage_edit', {'id': voyage.id}) }}">edit</a>

    {{ include('admin/voyage/_delete_form.html.twig') }}
{% endblock %}
