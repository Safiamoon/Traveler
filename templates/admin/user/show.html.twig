{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ include('admin/user/roles_pills.html.twig', { roles: user.roles}) }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('admin_user_index') }}" class="btn btn-primary mr-3">back to list</a>

    <a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="btn btn-primary mr-3">edit</a>

    {{ include('admin/user/_delete_form.html.twig') }}
{% endblock %}
